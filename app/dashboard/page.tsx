export default function Dashboard() {
  return (
    <div className="card">
      <h1>Dashboard</h1>
      <p>This is a placeholder dashboard that renders on Vercel with no backend.</p>
      <p className="small">You can extend this with auth, Firestore, etc., later.</p>
    </div>
  );
}
